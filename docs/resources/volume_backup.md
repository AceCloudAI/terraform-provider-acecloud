---
page_title: "acecloud_volume_backup Resource - acecloud"
subcategory: "Storage"
description: |-
  Manages an Ace Cloud volume backup.
---

# acecloud_volume_backup (Resource)

Manages an Ace Cloud volume backup.

## Example Usage

```terraform
resource "acecloud_volume_backup" "weekly" {
  name        = "weekly-backup"
  volume_id   = acecloud_volume.data.id
  description = "Weekly portable backup of data volume"
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) Name of the backup. Must be alphanumeric with hyphens.
- `volume_id` (String) ID of the volume to back up.

### Optional

- `description` (String) Human-readable description of the backup.
- `force` (Boolean) Set to `true` to back up an attached, in-use volume. Defaults to `false`. When the source volume is unattached, leaving this unset is sufficient.
- `snapshot_id` (String) ID of a snapshot to create the backup from (optional).

### Read-Only

- `created_at` (String) Timestamp when the backup was created.
- `id` (String) Unique identifier of the volume backup.
- `size` (Number) Size of the backup in GB.
- `status` (String) Current lifecycle status of the backup. Values: `creating`, `available` (backup is complete and restorable), `restoring`, `deleting`, `error`. Terraform waits until the backup reaches `available` before marking the resource as ready.
